# Roche Scientist Assistant — backend dependencies
# Pinned for reproducible installs across all 6 developers.
# Install (from the repo root):  pip install -r requirements.txt

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# --- LLM / RAG stack ---
langchain==0.3.14
langchain-groq==0.2.3
langchain-community==0.3.14
langchain-huggingface==0.1.2
chromadb==0.5.23
sentence-transformers==3.3.1

# --- PDF loading (for .pdf docs in mock_docs) ---
pypdf==5.1.0

# --- Language detection ---
langdetect==1.0.9

# --- Google Drive integration ---
google-api-python-client==2.156.0
google-auth==2.37.0

# --- HTTP client (ServiceNow + general) ---
httpx==0.28.1

# --- Config ---
python-dotenv==1.0.1
