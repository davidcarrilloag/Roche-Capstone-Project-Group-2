# Roche Scientist Assistant — backend dependencies (single integrated service)
# Install (from the repo root):  pip install -r requirements.txt
#
# Pinned to an internally consistent set (verified with `pip check`).

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# --- RAG (Gemini via Google AI Studio + ChromaDB) ---
langchain==0.3.14
langchain-core==0.3.63
langchain-text-splitters==0.3.8
langchain-google-genai==2.0.11
langchain-chroma==0.2.3
chromadb==0.5.23

# --- Language detection ---
langdetect==1.0.9

# --- HTTP client (ServiceNow) ---
httpx==0.28.1

# --- Google Drive document sync ---
google-api-python-client==2.156.0
google-auth==2.37.0

# --- SOP frontmatter parsing ---
PyYAML>=6.0

# --- Persistence (lab members, bookings) — SQLite via SQLModel ---
sqlmodel==0.0.22

# --- Config ---
python-dotenv==1.0.1
